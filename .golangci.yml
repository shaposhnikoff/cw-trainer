linters:
  enable:
    - govet
    - errcheck
    - staticcheck
    - gosimple
    - ineffassign
    - unused

linters-settings:
  errcheck:
    exclude-functions:
      - (*io.PipeWriter).Write

issues:
  exclude-rules:
    - path: _test\.go
      linters:
        - errcheck
